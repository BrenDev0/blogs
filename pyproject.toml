[project]
name = "blogs-app"
description = "App for adding blogs to websites"
requires-python = ">=3.12"
dependencies = [
    "python-dotenv==1.2.1",
    "fastapi==0.128.0",
    "uvicorn==0.40.0",
]

[project.scripts]
app = "src.app.main:app"

