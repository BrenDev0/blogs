[project]
name = "blogs-app"
version = "2.0.0"
description = "App for adding blogs to websites"
requires-python = ">=3.12"
dependencies = [
    "bcrypt== 5.0.0",
    "cryptography==46.0.3",
    "fastapi==0.128.0",
    "pydantic==2.12.5",
    "PyJWT==2.10.1",
    "python-dotenv==1.2.1",
    "sqlalchemy==2.0.45",
    "uvicorn==0.40.0",
]

[project.scripts]
app = "src.app.main:app"

